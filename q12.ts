let names0=["ali","amir","moon","nomi"];
let message0:string="It is very hot today,do'nt go out of the house."
for(let i=0;i<names0.length;i++){
    console.log(`hello,${names0[i]},${message0}`);
    
}

