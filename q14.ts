let list: string []=["star","moon","earth"];
let message:string="You are invited for a dinner.";
for (let f=0;f<list.length;f++){
    console.log(`${list[f]},${message}`);
    
    
}
export{list}